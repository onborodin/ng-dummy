var $ = window.$ = window.jQuery = require('jquery')
require('what-input')
require('foundation-sites/dist/js/foundation')

function foundation(item) {
        $(item).foundation()
}

export default foundation

