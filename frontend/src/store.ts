
import Vue from 'vue'
import Vuex from 'vuex'

Vue.use(Vuex)

function state() : any {
    return {
    }
}

const mutations : any = {
}

const actions : any = {
}

const getters : any = {
}

const store = new Vuex.Store({
    state: state,
    actions: actions,
    mutations: mutations,
    getters: getters
})

export default store
